import { Stack, Typography } from "@mui/material";
import React from "react";

const QSection = ({ control }) => {
  return (
    <Stack spacing={1.5}>
      <Typography>Question</Typography>
    </Stack>
  );
};

export default QSection;
