const useStyles = () => {
  const styles = {
    form: {
      inputLabel: {
        textAlign: "left",
        fontSize: "0.8rem",
        px: "5px",
        color: "#333",
        fontWeight: "bold",
      },
      primaryActionButton: {
        my: "2rem",
        py: "1rem",
      },
    },
  };
  return styles;
};

export default useStyles;
